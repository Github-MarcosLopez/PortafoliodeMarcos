import type { Resolver } from "../../types/resolver";
export interface FetchEvent {
    request: Request;
    waitUntil: (params: unknown) => Promise<void>;
}
export declare const kvResolver: Resolver;
